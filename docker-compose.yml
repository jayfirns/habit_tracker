version: "3.9"

services:
  backend:
    build:
      context: ./backend
    image: focusos-backend:latest
    container_name: focusos-backend
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
